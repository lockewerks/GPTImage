# fail2ban filter: match 401s in the GPTImage Caddy JSON access log.
# Install to /etc/fail2ban/filter.d/caddy-gptimage-401.conf
[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"status":401.*$
datepattern = "ts":{Epoch}
